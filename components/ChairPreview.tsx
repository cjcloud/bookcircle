import type { Book, Question } from '@/lib/types';
import { scoreLabels, scoreValues } from '@/lib/meeting';
export function Scale(){return <div className="scale">{scoreValues.slice(0,5).map(v=><span key={v}>{scoreLabels[v]}</span>)}</div>;}
export default function ChairPreview({book,questions,date}:{book:Book;questions:Question[];date?:string}){return <><div className="actions no-print"><span className="tag">{date?'Saved Chair version':'Draft preview · not final'}</span><button onClick={()=>window.print()}>Print / Save PDF</button></div><div className="preview"><h1>{book.book_title}</h1><p>{book.author}</p><h2>A few things to think about before we meet</h2><p>There’s no right answer — these are simply different ways into the book. We can see where everyone lands when we’re together. The Chair is free to use or skip any prompt.</p>{questions.map((q,i)=><article className="card" key={q.id}><span className="eyebrow">{q.optional?'Going Deeper · Optional':`Question ${i+1}`}</span><h2>{q.proposition}</h2><p><i>{q.subtext}</i></p>{!q.optional&&<Scale/>}</article>)}</div></>;}
