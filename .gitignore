node_modules/
.next/
.pnpm-store/
*.tsbuildinfo
.env*
.vercel/

.test-build/
.test-build*/
.npm-cache/

.research-cache/
.research-build*/
.dev-server.*.log
book-club-briefing-claude-handoff-*.zip

# Supabase CLI's own local scratch/cache state (project ref, pooler URL,
# component versions) — regenerated by `npx supabase link`/`db push` every
# time, never something to version. Nothing sensitive was ever in here
# (checked: no password in pooler-url), just noise worth not committing.
supabase/.temp/
